# E2E tests (removed)

End-to-end tests under this package were **removed** during the break/hook/watch architecture rework.

`make test-e2e-*` targets are **no-ops** (exit 0) until new e2e is reintroduced.
