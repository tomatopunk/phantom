# Binaries
/phantom
/phantom-agent
/phantom-cli
*.exe

# Generated
*.pb.go
*.pb.gw.go
bpf/*.o
bpf/*.bpf.o
bpf/*.skel.h

# IDE / OS
.idea/
#.vscode/
.DS_Store

# Env / secrets
.env
*.token
.cursor

# Builder
phantom-agent
phantom-cli

# Node / npm (e.g. src/desktop) — do not commit; run npm install locally / in CI
node_modules/
src/desktop/dist/

# Rust (workspace at repo root)
/target/

# e2e uprobe helper (built by make build-uprobe-e2e-helper)
test/e2e/uprobe_helper/uprobe_helper